{
  "name": "lull-site",
  "private": true,
  "version": "2.0.0",
  "description": "Lull — landing + installable web app (Cloudflare Worker + static assets)",
  "scripts": {
    "build": "node build.mjs",
    "build:ios": "node build.mjs && node build-ios.mjs",
    "deploy": "npm run build && wrangler deploy",
    "dev": "npm run build && wrangler dev"
  },
  "dependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "0.21.5",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "lucide-react": "0.383.0"
  }
}
